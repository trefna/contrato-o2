{
  "name": "contrato-o2",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client/admin-frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "nodemailer": "^6.4.11",
    "pdfkit": "^0.11.0",
    "dotenv": "^8.2.0"
  },
  "engines": {
    "node": "20.x"
  }
}
